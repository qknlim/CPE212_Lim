all:
  children:

     ubuntu:
       vars:
        ansible_user: kenzo
       hosts:
          server1:
            ansible_host: 192.168.56.7
            os_type: Ubuntu
          server2:
            ansible_host: 192.168.56.8
            os_type: Ubuntu

     centos:
       vars:
        ansible_user: kenzo
       hosts:
          server3:
            ansible_host: 192.168.56.9
            os_type: CentOS

     linuxservers:
        children:
          ubuntu:
          centos:

     servers:
       hosts:
          server1:
          server3:
